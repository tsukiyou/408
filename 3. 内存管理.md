# 内存管理概念
### 内存管理的基本原理和要求
###### 内存管理的主要功能
- 内存空间的分配与回收
- 地址转换
- 内存空间的扩充
- 内存共享
- 存储保护

##### 逻辑地址和物理地址
地址重定位：通过地址转换将逻辑地址转换为物理地址
#note 操作系统通过内存管理部件（MMU）将逻辑地址转换为物理地址

#### 程序的链接和装入
源程序变为内存中执行程序的步骤
- 编译：由编译程序将用户源代码编译成若干目标模块
  ![[168861217495563022(1).png|180]]
- 链接：由链接程序将目标模块与所需的库函数链接，形成完整的装入模块
  ![[168861217495563022(1) 2.png|180]]
- 装入：由装入程序将装入模块装入内存运行
  ![[168861217495563022(3).png|180]]

  ![[168861217495563022(4).png|350]]
##### 装入方式
###### 绝对装入
**绝对装入**：编译程序产生绝对地址的目标代码，装入程序按照绝对地址将程序与数据装入内存
	程序中逻辑地址与实际内存地址完全相同
	![[120837917495553162(1) 1.png|300]]
#note 只适用于单道程序环境

绝对地址可在`编译/汇编`时给出，也可由程序员直接赋予
#note 通常程序中采用符号地址，`编译/汇编`时转换为绝对地址
###### 可重定位装入
**可重定位装入**：程序中使用的指令和数据的地址是相对于程序始址（0）的逻辑地址，在模块装入时根据装入位置修改地址
	![[Pasted image 20250612151413.png|300]]
	#note 可重定位装入也称**静态重定位**
	#note 地址转换在装入时一次完成
		#note 装入后，不能在内存中移动，也不能申请内存空间（装入后地址固定
###### 动态运行时装入
**动态运行时装入**：程序中使用的指令和数据的地址是相对于**程序始址**（0）的逻辑地址，装入内存时，并不会将相对地址转换为绝对地址，在程序执行时转换为实际物理地址，需要**重定位寄存器**的支持
	![[Pasted image 20250612151455.png|300]]
	#note 动态运行时装入也称**动态重定位**
	#note 允许程序在内存中移动（在运行时候才将逻辑地址转换为物理地址
		#qes 为什么动态运行时装入允许程序在内存中移动
	#qes 动态运行时装入地址转换什么时候进行？
		
##### 链接方式
###### 静态链接
在程序运行之前，将各目标模块和它们所需的库函数链接成一个完整的装入模块
静态链接过程：
1. 修改各个模块的相对地址位链接后的地址
2. 变换外部调用符号位相对地址

###### 装入时动态链接
将目标模块装入内存时，采用边装入边链接的方式
![[173515817495564172(1).png|300]]
###### 运行时动态链接
在运行时需要某目标模块时，再对它进行链接。
	![[178409517495564622(1).png|300]]
	#qes 运行时未被用到的模块
		#note 未用到的模块，不会被调入内存和链接到装入模块

##### 进程的内存映像
#qes 内存映像各个要素都存放什么？
进程的内存映像相关要素：
- 代码段
- 数据段：程序运行时加工处理的对象，包括**全局**变量和**静态**变量
- 进程控制块（PCB）
- 堆：存放动态分配的**变量**（函数内的变量），**向高地址**分配空间
- 栈：**实现函数调用**，从用户空间**向低地址**方向增长
#note 代码段和数据段在调入内存时就指定了大小，固定不变
#note 堆和栈大小可动态变化
![[Pasted image 20250612184919.png|300]]
##### 内存保护
**内存保护**：确保每个进程的内存空间独享
1. 在CPU中设置一对上下限寄存器
2. 采取重定位寄存器和界地址寄存器进行越界检查。
	将逻辑地址与界地址寄存器进行比较，若未发生越界：重定位寄存器+逻辑地址=**物理地址**
	#note 界地址寄存器也称`限长寄存器`
##### 内存共享
#note 只有只读的区域才可以共享
##### 内存分配与回收
## 连续分配管理方式
**内部碎片**：程序小于分区大小，分区内部空间浪费
**外部碎片**：内存中的某些空闲分区由于太小而难以利用
#### 单一连续分配
**单一连续分配**：系统被分为系统区和用户区，**系统区**通常位于低地址部分，**用户区**仅有一道用户程序，即用户程序**独占**用户区

**优点**：无外部碎片，无需内存保护（仅有一道程序
**缺点**：只能用于单用户单任务的操作系统；有内部碎片；内存利用率低
#### 固定分区分配
**固定分区分配**：将内存划分为若干**固定**大小的分区，每个分区只装入一道作业
- 分区大小相等
- 分区大小不等
#qes 固定分区分配怎么记录分区分配情况
	分区使用表
存在的问题：
1. 分区太大放不进任一个分区
2. 有内部碎片
#### 动态分区分配
**动态分区分配**：进程装入内存时，根据进程实际的需要，动态地分配内存，使分区大小正好适合进程的需要
#note 动态分区会产生外部碎片

动态分区回收方法：
- 有相邻空闲分区则**合并**，修改已有表项（起始地址/大小）
- 无相邻空闲分区，新建表项，填写始址和大小
#### 分配算法
##### 基于顺序搜索的分配算法
###### 首次适应算法
首次适应算法：空闲分区按**地址**递增的次序排列，分配内存时，顺序查找到**第一个**能满足的大小空闲分区
- 保留了高地址部分的大空闲分区，有利于大作业的装入
- 内存低地址部分出现很多小碎片，每次查找都要经过，开销大
###### 临近适应算法
临近适应算法：由首次适应算法演变而来，但分配内存时从**上次查找结束**的位置继续查找。
- 不用每次从起始地址查找，绕过了小碎片
- 使高地址低地址的分区以同等概率分配，导致大分区都被划分为小分区，没有大分区可用
###### 最佳适应算法
最佳适应算法：空闲分区按**容量**递增的次序排列，分配内存时，顺序查找到第一个能满足大小的空闲分区
#note 分区链由小到大排列，每次分配后动态调整，实现由小到大
- 性能差，每次分配会留下越来越多的**外部碎片**
###### 最坏适应算法
最坏适应算法：空闲分区按容量递减的次序排列，分配内存时，查找到第一个能满足大小的空闲分组（最大的空闲分组），从中分割一部分空间给进程。
#note 分区链由大到大排列，每次分配后动态调整，实现由大到小
- 很快没有大空闲分区可用，性能差
##### 基于索引搜索的分配算法
#todo 
## 基本分页存储管理

# 虚拟内存管理
## 虚拟内存的基本概念
#### 传统存储管理方式的特征
- 一次性：作业必须一次性全部装入内存后，才能开始运行
- 驻留性：作业被装入内存后，就一直驻留在内存，直到作业运行结束
#### 局部性原理
- [[3.存储系统#^7c4c78|时间局部性]]
- [[3.存储系统#^7c4c78|空间局部性]]
#### 虚拟存储器的定义和特征
虚拟存储器**实现思想**：基于局部性原理，仅需将程序当前运行用的页面装入内存，其余部分暂留外存，需要时再调入

虚拟存储器的特征
- 多次性：允许被分成多次调入内存，只需要当前允许的部分装入内存即可运行
- 对换性：允许将暂时不使用的程序和数据从内存调至外存，需要时再换入
- 虚拟性：从逻辑上扩充了内存的容量，使用户看到的内存容量大于实际容量

#### 虚拟内存技术的实现
虚拟内存的实现建立在离散分配的内存管理方式的基础上
	#qes why?
		将一个作业多次调入内存采用连续分配方式不方便实现

虚拟存储器通过请求调页和页面置换功能实现
- **请求调页**：当访问的信息不在内存时，由操作系统将所需信息由外存调入内存，然后继续执行程序
- **页面置换**：当内存空间不够时，将内存中暂时不用的信息换出到外存，从而腾出空间存放调用内存的信息

虚拟内存实现需要的硬件支持
- [[#页表机制|页表机制]]，作为主要的数据结构
- [[#缺页中断机构|中断机构]]
- 地址变换机构
## 请求分页管理方式
#### 页表机制
为了实现请求调页功能，请求页表项相对于页表项增加了4个字段
![[Pasted image 20250613165304.png|450]]
- 状态位P：标记该页是否调入内存
- 访问字段A：记录在一段时间内被访问的次数
- 修改位M
- 外存地址
#### 缺页中断机构
缺页中断：当访问的页面不再内存中便产生缺页中断，请求操作系统的`缺页中断处理程序`处理
 - 一条指令执行期间可能产生多次缺页中断
#### 地址变换机构
地址变换过程：先快表，再页表，再外存（缺页中断）可得**物理块号+页内地址=物理地址**，调入后更新页表/快表
1. 检索**快表**，**若命中**则从相应表项取出该页的**物理块号**，并修改页表项中的访问位和修改位
==快表未命中后⬇==
2. 检索**页表**，若找到则从相应表项取出**物理块号**，并**更新快表**（将该页表写入快表
	写入快表时若快表已满则采用某种替换算法替换
==页表未命中后⬇==
3. **缺页中断处理**，请求调页，页面调入后**更新页表和快表**
4. 用物理块号和页内地址拼接**形成物理地址**，用该地址访存
![[Pasted image 20250613225212.png|550]]
## 页面置换算法
#note 缺页中断与页面置换次数的区分
	缺页中断是发现某个页面没有请求调页的次数
	页面置换是调入的页面与已存入数据的页面置换
		即页面置换发生在页面占满时，选择一个已存入数据的页面置换掉
#### 最佳（OPT）置换算法
**最佳置换算法**：淘汰以后`最长时间内`不再被访问的页面
#qes 给出访问页面串，如何得到最佳置换算法淘汰的是哪个？
	若可分配物理块有n个
	从访问页面串向后数，数到`n-1`个物理块对应的值，则没找到的即为淘汰的页面
	（即**哪个最后出现**

#eg 最佳置换算法
![[Pasted image 20250613230017.png|500]]
	![[Pasted image 20250613230854.png|600]]
#### 先进先出置换算法
**先进先出置换算法**：淘汰最早进入内存的页面--将页面按照调入的先后顺序排成一个队列，换出时选择队头的页面
![[Pasted image 20250613230017.png|500]]
![[Pasted image 20250614164711.png]]
#note 只有先进先出算法会产生belady异常：随着为进程分配的物理块增多，缺页次数不减反增
	不同分配物理块运行到后面页面完全不同
#### 最近最久未使用
**最近最久未使用（LRU）置换算法**：为每个页面设置一个访问字段，记录页面自上次被访问以来所经历的时间，淘汰时间最长（最久未访问）的页面
#note 实现需要寄存器和栈的硬件支持
#qes 手动做题时，给出访问页面串，如何得到LRU算法淘汰的是哪个？
	若可分配物理块有n个
	从访问页面串向前数，数到`n-1`个物理块对应的值，则没找到的即为淘汰的页面
	（即**逆向向前，哪个最后出现**

#eg 
![[Pasted image 20250613230017.png|500]]
![[Pasted image 20250614165710.png]]

#### 时钟置换算法
#todo 

## 页框分配

#### 驻留集，抖动和工作集
驻留集：给一个进程分配的页框的集合
抖动：刚刚换出的页面马上要换入内存，刚刚换入的页面马上要换出内存
工作集：某段时间间隔内进程要访问的页面集合
	工作集可根据工作集窗口尺寸确定---落在窗口尺寸内的页面
驻留集大小不能小于工作集大小，否则会频繁缺页

#### 物理块分配算法
- 平均分配算法
- 按比例分配算法：按照进程的大小按比例分配物理块
- 优先权分配算法：为重要和急迫的进程分配较多的物理块
#### 内存分配策略
##### 固定分配局部置换
为每个进程分配固定数量的物理块，运行期间**不变**。若运行中**发生缺页**，则在已分配的页面中选择一页换出
#qes 为什么固定分配没有全局置换？
	固定分配必然物理块数量固定，因此不能够从空闲物理块中取出
##### 可变分配全局置换
为每个进程分配一定数量的物理块，运行期间数量**可变**。若**发生缺页**，则从空闲物理块队列中取出一块分配给该进程，并将所缺页面调入。
	只要进程发生缺页，都将获得新的物理块
#note 当空闲物理块用完时，系统会从任意一个进程中**调出**一个未锁定的页--该进程物理块会减少

##### 可变分配局部置换
为每个进程分配一定数量的物理块，当某进程**发生缺页**时，在已分配的页面中选择一页换出。
	但当**频繁缺页**时，则为该进程分配若干空闲物理块，直到该进程缺页率达到适当程度。
	**缺页率特别低**时，减少分配给该进程的物理块


#### 调入页面的时机
###### 预调页策略
预测不久后被访问的页面，将它们预先调入内存
#note 主要用于进程的首次调入，由程序员指出调入哪些页，使预调入的页访问率提高
###### 请求调页策略
当访问的页面不在内存时，便提出请求，由系统将其所需页面调入内存
#### 从何处调入页面
文件区：用于存放文件，采用**离散分配**方式
对换区：用于存放对换页面，采用**连续分配**方式
##### 拥有足够的对换区空间
**从对换区调入**所需页面，以提高调页速度，因此在程序运行前，将于该进程有关的文件从文件区复制到对换区
##### 缺少足够的对换区空间
- 不会被修改的文件直接从文件区调入（不会被修改因此不用换出
- 可能被修改的文件，换出时放在对换区，后续从对换区调入（对换区增快写速度
##### UNIX方式
未运行的页面从文件区调入，换出时换出到对换区，后续从对换区调入

