# 操作系统的概念
计算机系统自下而上可分为4部分：[[408/2.计算机组成原理/1. 计算机系统概述#计算机硬件|硬件]]，操作系统，应用程序，用户

>操作系统：控制和管理整个计算机系统的硬件和软件资源，组织、调度计算机的工作与资源的分配，进而为用户和其他软件提供接口与环境的程序集合

## 操作系统的功能和目标
#### 操作系统作为计算机系统资源的管理者
- 处理机管理
	![[Pasted image 20250604173851.png|550]]
- 存储器管理
	![[Pasted image 20250604173933.png|550]]
- 文件管理
	![[Pasted image 20250604174000.png|550]]
- 设备管理
	![[Pasted image 20250604174020.png|550]]
#### 操作系统作为用户与计算机硬件系统之间的接口
操作系统提供的接口可分为**命令接口**和**程序接口**
1. 命令接口：利用这些操作命令来组织和控制作业的执行
	- 联机命令接口：用户输入一条命令，操作系统就解释并执行该命令，完成后用户再输入下一条命令
	- 脱机命令接口：事先把一组作业控制命令写成脚本，提交给操作系统自动按顺序执行，运行期间不与用户交互

2. 程序接口-可用来请求操作系统服务
	程序接口由一组系统调用组成，用户通过这些系统调用请求操作系统的服务
		#note 系统调用也可称为广义指令
	
	**GUI**：图形用户界面即图形接口
		GUI最终是通过调用程序接口实现
		#qes 图形接口是否是操作系统的一部分？
			
#### 操作系统实现对计算机资源的扩充
[[408/2.计算机组成原理/1. 计算机系统概述#^fb5a38|裸机]]在最内层，构成计算机系统的物质基础，外面是操作系统
操作系统所提供的资源管理功能和各种服务功能将[[408/2.计算机组成原理/1. 计算机系统概述#^fb5a38|裸机]]改造成功能更强、使用更方便的机器

虚拟机：覆盖了软件的机器
	#note 也可称为扩充机器 ^d68b0c


## 操作系统的特征
##### 并发
- 并发：多个事件在同一**时间间隔**内发生
	![[3-1(2).png|200]]
- 并行：**同一时刻**能进行多个工作
	![[3-1(1).png|200]]

  
##### 共享
共享：资源共享，系统中的资源可供内存多个并发执行的进程共同使用
1. 互斥共享方式
	#eg qq和微信视频，同一时间段摄像头仅能分配给一个进程
2. 同时访问方式

并发和共享是操作系统最基本的两个特征
##### 虚拟
虚拟：将物理上的实体编程若干逻辑上的对应物
虚拟技术分别有时分复用技术和空分复用技术
#eg 时分复用技术：虚拟处理器
	例如CPU分时共享。具体来说，在时分复用技术下，操作系统将CPU的时间片分为多个部分，每个时间片都被分配给不同的任务执行
#eg 空分复用技术：虚拟存储器
	将内存中暂时不用的数据移动到硬盘上，以便腾出更多的内存空间供其他程序使用。
##### 异步
异步：多个程序并发执行，由于资源有限，进程的执行不是一贯到底的，而是走走停停，以不可预知的速度推进

# 操作系统发展历程
### 手工操作阶段​ 
这个阶段没有操作系统，所有的工作都需要人工干预
![[4-1(1).png|450]]
手工操作阶段两个问题：
- 所有的资源都被一个用户占用，CPU、磁盘、外设不能完全利用，资源利用率低
- CPU等待手动操作，CPU利用不充分

### 批处理阶段
#### 单道批处理系统
>单道批处理系统：将一批作业以脱机方式输入磁带，在操作系统监督程序的控制下使这批作业连续处理
>![[4-1(2).png|450]]

单道批处理系统主要特征：
1. 自动性：磁带上的作业能自动的逐个运行，无序人工干预
2. 顺序性：各作业顺序进入内存，按顺序依次运行
3. 单道性：内存中只有一道程序运行

缺点：内存中只能存放一道作业运行，CPU大量时间用于等待I/O
#### 多道批处理系统
>多道批处理系统：用户预先提交作业，并在管理程序的控制下穿插运行，共享系统中的软硬件资源，实现各部件之间的并行工作（当某个程序不使用CPU时，CPU转去执行另一条程序
>![[4-1(3).png|300]]

多道批处理系统主要特征：
1. 多道：内存中同时存放多道相互独立的程序
2. 宏观上并行：进入系统的多道程序并发运行
3. 微观上串行：内存内多道程序轮流占有CPU，交替执行

缺点：用户响应时间长，**无人机交互能力**
#note 多道批处理系统仅是实现预先提交作业的并行工作，但没有交互能力
### 分时操作系统
分时操作系统：CPU以时间片为单位轮流为各作业服务，任务按照循环队列排队，时间片结束则执行下一条任务
#qes 分时操作系统
分时操作系统系统主要特征：
1. 同时性：多个终端用户同时使用一台计算机
2. 交互性：可进行人机交互
3. 独立性：多个用户彼此独立的进行操作，互不干扰，感受不到别人的存在
4. 及时性：用户请求可在短时间得到相应
	#note 分时操作系统根据用户数和时间片仍需一定的响应时间

### 实时操作系统
实时操作系统：在**严格时间限制内对外部事件作出响应**的操作系统，而不需要时间片排队
	硬实时系统：某个动作必须在规定的时刻发生
		#eg 飞行器的飞行自动控制系统
	软实时系统：可偶尔违反时间规定
		#eg 飞机订票系统，银行管理系统

实时操作系统的主要特点：及时性和**可靠性**

### 其他操作系统
- 网络操作系统
	![[Pasted image 20250605161517.png]]
- 分布式操作系统
	![[Pasted image 20250605161601.png]]
- 个人计算机系统
	![[Pasted image 20250605161652.png]]

# 操作系统的运行环境
## 处理器运行模式

>特权指令和非特权指令
- 特权指令：不允许用户直接使用的指令，如I/O指令，关中断指令，修改程序状态字寄存器等的指令
- 非特权指令：用户直接使用的指令，不能直接访问软硬件资源
	#eg 加减法指令

>内核程序和应用程序	

**内核**管理着系统的各种资源，是操作系统最重要的部分，最靠近硬件，可视为软硬件之间的桥梁 ^6bd7f5
- 操作系统内核程序
	可使用特权指令
- 用户自编程序（应用程序）
	仅允许使用非特权指令

#qes 如何判断此时是否可以执行特权指令，运行的是内核程序还是应用程序？
	将CPU的运行模式划分为内核态和用户态两种状态
>内核态和用户态
- 处于内核态时：此时正在运行的是内核程序，可以执行特权指令
- 处于用户态时：此时正在运行的是应用程序，仅能执行非特权指令

**访管指令**：应用程序请求操作系统服务的指令
#note 访管指令是非特权指令（在用户态执行，仅能执行非特权指令

##### 内核态、用户态的转换
- **内核态→用户态**：执行特权指令，修改PSW的标志位为用户态
	#note 切换到用户态的指令是特权指令（在内核态发出
- 用户态→内核态：只能通过**中断或异常**引发，硬件自动完成```用户态→内核态```的转换
	#note **中断/异常**是实现```用户态→内核态```的唯一途径
	#note 需要操作系统介入的地方，都会触发中断信号
		需要操作系统服务，则需要进入内核态，而进入内核态需要中断/异常


#### 内核功能四方面
内核程序如何管理资源呢？可以将内核的功能分为四方面：​ 
- **时钟管理**：时钟是计算机部件中最关键的设备。很多方面的功能都要依赖于时钟。​
- **中断机制**：只有一小部分功能属于[[#^6bd7f5|内核]]，主要负责保护和恢复中断现场的信息。​ 
- **原语**：原语是一些**接近硬件**的使用频繁的公用小程序，运行时具有**原子性**，是一气呵成的。操作系统内核中包含着很多原语，例如进程调度、进程通信中的部分操作都可以定义为原语。​ 
	#qes 如何实现原语的原子性？
	关中断指令+开中断指令：中间无法中断
- **系统控制的数据结构和处理**：为了对系统进行管理，系统中记录着很多结构化的数据，例如进程控制块（PCB），设备控制块（DCB）。
 ![[8-1(1).png|500]]

## 中断和异常的概念
#qes 引入中断对多道操作系统的意义是什么？
	在某道程序未使用某种资源（CPU，other...）时，释放该资源的占有权，需要时通过中断请求
	使CPU在I/O期间可以执行其他操作-提高利用率

##### 中断和异常的定义
- 中断（外中断）：来自CPU执行指令外部的事件，通常为信息输入/输出
	#eg I/O中断，时钟中断
- 异常（内中断）：来自CPU执行指令内部的事件
	#eg 非法操作码，地址越界....
	#note 异常不能被屏蔽，一旦出现应该立即处理

[[如何区分中断和异常]]

##### 中断和异常的分类
>外中断
>	可屏蔽中断：**INTR**线发出的中断请求
>	不可屏蔽中断：**NMI**线发出的中断请求

>异常
>	**故障**：**指令**执行时由于**可修复的问题**（如缺页、非法操作码等）而被中断，**操作系统处理完后可以重新执行指令继续运行**
>	
>	**自陷**：由**陷入指令**引发，用于**用户态**下调用操作系统内核程序
>	
>	**终止**：出现了CPU无法执行的**硬件故障**，且是**无法修复的错误**（如控制器出错），则直接**终止该进程**

#note 故障异常和自陷异常属于**软件中断**
#note 终止异常和外部中断属于**硬件中断**

![[Pasted image 20250606093450.png|300]]

##### 中断和异常的处理过程
1. 检测到异常/中断
2. 打断当前的用户程序，转到相应的中断/异常处理程序
3. 处理中断/异常
	- 若能解决相应的问题，则回到应用程序**继续执行**
	- 若是不可恢复的致命错误，则**终止**用户程序
###### 中断处理和子程序调用的比较
1. **中断处理程序**和被中断的程序是相互独立的；**子程序**和主程序使同一程序的两个部分，属于主从关系
2. **中断**的产生是随机的；**子程序调用**是通过调用指令引起的，是由程序设计者事先安排的
3. **中断处理**需要有专门的硬件电路；**子程序调用**属于软件处理过程，无需硬件
4. **中断处理程序**的入口地址可由硬件向量法产生；**子程序**的入口地址由CALL指令的地址码给出
5. **中断处理程序**对PC的保护由中断隐指令完成；**子程序调用**对PC的保护由CALL指令完成
6. 响应**中断**时，需要对多个中断请求进行裁决，调用**子程序**不需要
## 系统调用
系统调用是操作系统提供给应用程序（程序员）使用的接口，凡是与共享资源有关的操作都必须通过系统调用向操作系统发出服务请求

>系统调用的功能：
- 设备管理：设备的请求/释放/启动等功能
- 文件管理：文件的读/写/创建/删除等功能
- 进程控制：进程的创建/撤销/阻塞/唤醒等功能
- 进程通信：进程之间的消息传递/信号传递等功能
- 内存管理：内存的分配/回收等功能

##### 系统调用的过程
1. （用户程序）将系统调用号和所需的参数压入堆栈，执行陷入指令
2. 保护中断现场：
3. 分析调用类型，转入相应的系统调用处理子程序
	通过系统调用入口表可以找到系统调用子程序的入口地址
4. 执行结束，恢复被中断/新进程的CPU现场，返回被中断进程/新进程
![[Pasted image 20250606122100.png]]

# 操作系统结构
#### 分层法
分层法：将操作系统分为若干层，底层为硬件，顶层为用户接口，每层只能调用**相邻低层**的功能和服务
	#note 分层法重要的特点：每层只能调用紧邻它的低层的功能和服务，即单向依赖。

>分层结构的优缺点：​
- 分层结构优点：
	- 便于调试和验证：自底向上逐层调试，确保底层正确再调试上层
	- 易扩充维护：可增加/修改/替换某层时，只要不改变层间的接口，就不会影响其他层|
- 分层结构缺点：
	- 难以合理定义各层边界：可能存在双向依赖，难以分层
	- 效率差：执行一个功能需要逐层传递通信

#### 模块化
模块化：将操作系统按照功能划分为若干具有一定独立性的模块，且规定好各模块之间的接口，使各模块直接能通过接口进行通信。
	![[Pasted image 20250606143720.png|400]]

📌 在划分模块时，要充分考虑模块的独立性问题，模块独立性越高，各模块间的交互就越少，系统的结构也就越清晰。

>衡量模块的独立性有两个标准：​ 
- 内聚性：用于衡量模块内部各部分间联系的紧密程度。内聚性越高，模块独立性越好。​ 
- 耦合度：用于衡量模块间相互联系和相互影响的程度。耦合度越低，模块独立性越好。​

>模块化优缺点：​
- 优点：
	- 模块间逻辑清晰，易于维护，提高了正确性
	- 增强了操作系统的可适应性(支持动态加载新模块
	- 并行开发，可加速操作系统的开发过程。​ 
- 缺点：​ 
	- 模块间的接口规定很难满足对接口的实际需求。​ 
	- 并行开发，各模块设计者齐头并进，难以协调。



📌 从操作系统的内核架构划分，可以分为宏内核和微内核。​ ​ 
#### 宏内核
宏内核​ ​：操作系统的**主要功能模块**都作为一个紧密联系的整体运行在核心态，从而为用户提供高性能的系统服务。
	![[8-1(2).png|300]]
	也称作单内核或大内核。

>宏内核优缺点：​
- 优点：各模块之间共享信息，能有效利用相互之间的有效特性，因此具有**高性能**。​ 
- 缺点：内核代码庞大，结构混乱，难以维护

📌 宏内核操作系统：Windows、Android、iOS、macOS

#### 微内核
**微内核架构**：将内核中**最基本的功能**保留在内核，将不需要在核心态执行的功能转移到用户态执行，从而降低内核的设计复杂性。
	被移除内核的操作系统代码被划分为若干服务程序，并且它们的执行相互独立，交互则借助于微内核进行通信。
	![[8-1(3).png|300]]​ 

在微内核架构下，操作系统被划分为两部分：**微内核**和多个服务器。
>**微内核**：能实现操作系统最**基本核心功能**的小型内核，包含：​ 
-  与**硬件处理**紧密相关的部分​ 
-  一些较基本的功能​ 
-  客户与服务器/之间的通信

>微内核架构的多个服务器
 除此之外，操作系统中的绝大部分功能都放在微内核外的一组服务器进程中实现，例如：用于管理线程/进程的进程/线程服务器；用于管理虚拟存储器的虚拟存储器服务器。​ ​ 
	📌 注意，服务器进程运行在用户态！​ 在微内核结构中，为了实现高可靠性，只有微内核运行在内核态，其余模块（包含用户进程以及服务器进程）都运行在用户态。因此，服务器进程的崩溃并不会使得操作系统的崩溃。

##### 微内核的基本功能
微内核结构常用“**机制与策略分离**”的原理来构造OS结构，将**机制**部分以及与**硬件紧密相关**的部分放入微内核。
	微内核操作系统可以将进程管理、存储器管理以及I/O管理这些功能一分为二，属于机制的很小一部分放入微内核，而绝大部分放入微内核外的服务器进程实现，大多数服务器都要比微内核大。​ 
	机制通常描述了一个系统中实现某些特定功能的底层技术和方法，如数据存储、信息传输、安全防护和资源管理等。机制的设计应该是通用、可靠、高效的，这样就可以满足很多不同的应用场景。​ 
	策略则是描述了使用这些机制来满足特定需求的规则和决策，其主要关注的是**如何使用机制来实现用户的需求**。策略的实现应该是具体、可配置的，可以根据不同的需求进行调整和修改。
​ ​ 
1. 进程（线程管理）
	#note 在进程管理中设置一个/多个进程有限集队列属于**机制**问题，应放入微内核
	#note 进程如何分类，优先级的确认方式属于**策略**问题，可将它们放入微内核外的进程服务器中
2. 低级存储器管理
	#note 实现逻辑地址转换为物理地址的页表机制和地址变换机制，**依赖于硬件**，应放入微内核
	#note 采取何种页面置换算法，采用何种分配与回收策略，是实现虚拟存储器管理的**策略**，应放于存储器管理服务器中
3. 中断和陷入处理
	微内核主要是捕获中断/陷入事件，并进行中断响应处理。处理过程则发送给相关的服务器处理

📌 微内核采用的是`客户/服务器模式`。

##### 微内核的特点
>优点：​ 
-  **扩展性和灵活性**：许多功能从内核中分离出来，当要对功能进行修改或增加时，只需要在服务器中进行相应的修改，而无需改动内核。​ 
-  **可靠性和安全性**：微内核外的模块运行在用户态，模块中的错误不会使整个系统崩溃
- **可移植性**：与CPU和硬件相关的代码放在微内核中，而服务器与硬件平台无关，因此将操作系统移植到另一个另一个硬件平台上时，只需要对微内核做修改，所需做的修改量很小。​ 
-  **分布式计算**：客户进程与服务器进程之间、服务器进程与服务器进程之间的通信采用消息传递机制，使得微内核系统能很好地支持分布式系统和网络系统。​ 

>缺点：​ 
-  性能不佳：运行时需要频繁地在核心态和用户态之间进行切换，操作系统的执行开销较大。​ ​ 

📌 受益于微内核架构的可靠性和安全性，其在实时、工业、航空以及军工领域应用非常
![[8-1(4).png]]


#### 外核
>**外核**：在内核态运行的程序，任务是为虚拟机分配未经抽象的资源，并保证资源使用的安全性
![[9-1(1).png|400]]
优点：减少了资源的映射层，提高效率
	#note 直接分配未经抽象的资源，外核只需要记录已分配给各个虚拟机的有关资源

## 操作系统引导
操作系统引导：计算机利用CPU运行特定程序，通过该程序逐步识别硬盘-硬盘分区-识别分区上的操作系统，最后启动操作系统

#### 操作系统引导过程
1. 激活CPU读取ROM中的boot程序并执行BIOS（基本输入输出系统）的指令
2. 硬件自检
3. 加载带有操作系统的硬盘：将控制权交给启动顺序第一的存储设备，将该存储设备引导扇区的内容加载到内存
4. 加载**主引导记录（MBR）**：
	主引导记录：包含硬盘分区表，作用是告诉CPU去硬盘的哪个主分区（活动分区）去找操作系统
		硬盘以特定的标识符区分引导硬盘，若不是可引导盘就去检查下一个存储设备，若无其他启动设备，则会死机
5. 扫描硬盘分区表，并加载硬盘活动分区
	根据硬盘分区表找到含有操作系统的硬盘分区（活动分区）
6. 加载**分区引导记录（PBR）**：
	分区引导记录（PBR）：活动分区的第一个扇区，作用是寻找并激活分区根目录下的**启动管理器**
	启动管理器：用于引导操作系统的程序
7. 加载启动管理器
8. 加载操作系统
![[10-1(1) 1.png|700]]
## 虚拟机
>虚拟机：通过虚拟化技术，将一台物理机器虚拟化为多台虚拟机器，隐藏其物理特性，为用户提供统一的、模拟的计算环境

### 第一类虚拟机管理程序
第一类虚拟机管理程序：拥有最**高特权级**，直接运行在**物理硬件**上，有对物理硬件的完全控制权，可直接分配物理资源
	![[11-1(2).png|450]]
- 支持的虚拟机数量：比第二类VMM更多
- 虚拟机的可迁移性：比第二类VMM更差

#####  操作系统特权指令执行
第一类虚拟机管理程序运行在最高特权级，可执行所有特权指令
	#note 管理程序以上皆运行在用户态

>第一代虚拟机管理程序上的操作系统特性
- 操作系统运行在**用户态**无法执行特权指令（通过陷入由虚拟机管理程序执行
- 虚拟内核态：操作系统认为自己运行在内核态（实际上不是）

#qes 第一代虚拟机管理程序中**操作系统**如何执行特权指令？
	通过 **陷入（trap）** 请求 VMM的服务。（由VMM代执行，达到同样的效果
		- VMM 捕获后 **模拟** 其效果（如更新影子页表、维护 I/O 队列），再将控制权返回来宾。
		- 因此来宾“看上去”好像成功执行了指令，但实际上是 VMM 在后台完成的。


### 第二类虚拟机管理程序
**第二类虚拟机管理程序**：运行在操作系统之上的一个进程，由宿主操作系统分配资源和执行指令
	![[11-1(3).png|400]]
#####  操作系统特权指令执行

>第二代虚拟机管理程序上的操作系统特性
- 操作系统/管理程序运行在**用户态**无法执行特权指令
- 虚拟内核态：操作系统认为自己运行在内核态（实际上不是）

#qes 第二代虚拟机管理程序中**客户操作系统**如何执行特权指令？
	由VMM截获GuestOS的系统调用，并转换为对宿主操作系统的调用。（由宿主操作系统代执行，达到同样的效果