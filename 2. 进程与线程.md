# 进程与线程
## 进程的概念和特征
#### 进程的概念
#qes 为什么要引入进程的概念？
	程序并发运行环境下，为了更好的描述和**控制程序的并发执行**，实现操作系统的并发性和共享性

- 进程：进程实体的运行过程，是系统进行资源分配和调度的一个**独立单位**
	- 进程是程序及其数据加载到内存后，正在CPU上执行的一个实例

###### 程序与进程的区别
- 程序是静态的，是存放在磁盘的可执行文件
- 进程是动态的，是程序的一次执行过程

#### 进程的特征
#note 特征不必记忆只要求理解
- 动态性：进程是程序的一次执行，它有着创建、活动、阻塞、终止过程，这就是进程的生命周期，进程是动态变化的。
- 并发性
- 独立性：进程是独立获得资源和接受调度的**基本单位**
- 异步性

### 进程的组成
进程由进程控制块（PCB）、程序段、数据段组成
##### 进程控制块
进程控制块（PCB）：操作系统用于标记**进程**的关键数据结构。记录了与该进程相关的**全部核心信息**，操作系统通过PCB对进程进行控制和管理
#note PCB是进程实体的一部分，是进程存在的唯一标志

###### 进程控制块在生命周期中的作用
- 进程创建时，操作系统为进程新建一个PCB
- 进程执行时，操作系统通过PCB了解进程的现行状态，以便进行控制
- 进程结束时，操作系统收回PCB，进程消亡
  git config --global user.email "YOUR_EMAIL"
###### PCB的构成
1. 进程描述信息
	进程标识符（PID）：标志各个进程
	用户标识符（UID）：进程共享的用户
		#note 用户标识符用于共享和保护服务
2. 进程控制和管理信息-状态，优先级等
3. 资源分配清单
4. 处理机相关信息：CPU各寄存器的值（也称CPU上下文），使进程可从断点继续执行
##### 程序段
##### 数据段

## 进程的状态与转换
#### 进程的状态
- 创建态：初始化PCB，分配进程所需资源
	#note 若进程创建所需的资源不能得到满足，则一直处于创建态
- 就绪态：获得除CPU以外的一切所需资源，等待CPU资源
- 运行态
- 阻塞态：进程等待某一事件而暂停运行
- 挂起态
	- 阻塞挂起
	- 就绪挂起
- 终止态：进程结束，进行资源释放和回收

#note 挂起和阻塞的区别？
	挂起态将进程映像调到外存
	阻塞态进程映像还在内存
#### 进程状态的转换
- 就绪态→运行态：获得CPU资源
- 运行态→就绪态：时间片用完/被高优先级进程剥夺
- 运行态→阻塞态：等待某一事件的发生
	#note `运行态→阻塞态`是进程通过系统调用请求操作系统服务的**主动行为**
- 阻塞态→就绪态：等待的事件到来
	`阻塞态→就绪态`是被动的行为，需要其他进程的协助
- 挂起态
	- 阻塞态↔阻塞挂起
	- 创建态/就绪态/运行态/阻塞挂起→就绪挂起→就绪态
![[19-2(1).png]]
#note 进程无法直接从`阻塞态→运行态`，要经过`就绪态`
#note 进程无法直接从`就绪态→阻塞态`，要在运行态（等待事件）才可转换为`阻塞态`
	因为转换为`阻塞态`是用户自身发出的请求，需要在运行才可发出请求

## 进程控制
#note 仅需理解，不必记忆

#qes 进程控制通过什么实现（是否可中断）？
	原语，不可中断
### 进程的创建
##### 引起进程创建的事件
用户登录，作业调度，系统提供服务，应用请求
#### 创建原语
1. 分配进程标志号，申请空白PCB
2. 分配运行所需的资源
3. 初始化PCB
4. 插入就绪队列（若就绪队列可插入--创建态→就绪态

### 进程的终止
##### 引起进程终止的事件
1. 正常结束
2. 异常结束
3. 外界干预：操作员或操作系统干预
#### 终止原语
1. 找到进程标志号对应的**PCB**，读出进程状态
	- 若处于运行状态，立即终止执行，将CPU分配给其他进程
	- 若有子孙进程，**通常**需要全部终止
2. **归还资源**-父进程/操作系统
3. 删除PCB

>**级联终止**：一个进程终止，其所有子进程也终止
### 进程的阻塞和唤醒
#### 阻塞原语
1. 找到进程标志号对应的**PCB**
2. 保护现场，将其转为阻塞态
3. 插入PCB至相应进程的等待队列
#### 唤醒原语
1. 找到进程标志号对应的**PCB**
2. 将PCB从等待队列中移除，设置状态为就绪态
3. 插入PCB至就绪队列
   
#note `Block原语`和`Wakeup原语`必须成对使用，因什么阻塞则因什么唤醒
## 进程通信
#### 共享存储
>**共享存储**：在通信的进程之间存在一块可以直接访问的**共享空间**，通过对这片共享空间进行读/写操作实现进程之间的信息交换
>![[15-2(1).png|300]]

操作系统负责为通信进程提供存储空间和同步互斥工具
数据交换由用户自己安排读写指令完成
#### 消息传递
>**消息传递**：进程间的数据交换以格式化的消息（Massage）为单位，通过操作系统提供的**发送原语**和**接收原语**进行数据交换

- 直接通信方式：发送进程**直接发送消息**挂至接收进程的**消息缓冲队列**上，接收进程从消息缓冲队列取得消息
- 间接通信方式：收发双方头通过中间实体进行消息传递
#### 管道通信
>**管道**：特殊的**固定大小**的共享**文件**，又称为pipe文件，数据在管道内是先进先出的

管道通信特性：
1. 管道读写同步特性
	- 管道**写满时**，写进程将被阻塞，直到读进程将管道内的数据取走，即可唤醒写进程
	- 管道**读空时**，读进程将被阻塞，直到写进程往管道内写入数据，即可唤醒读进程
2. 管道只能采用半双工通信
	#note 若要实现双向同时通信，需要设置两个管道
3. 进程要互斥的访问管道

#qes 管道和共享存储之间的区别？
	1. 管道按照循环队列先进先出的顺序读写，共享存储可以随意读写存储区的任何位置
#qes 管道多进程读写问题
	2. 一个管道允许多个写进程，一个读进程-以此为准
	3. 允许多个写进程，多个读进程，系统让读进程轮流从管道读数据
## 线程和多线程模型
### 线程的概念与特点
#### 线程的基本概念
#qes 为什么要引入线程？
	传统进程只能串行执行代码，无法并发执行多任务
	将进程中的不同任务分配到不同线程，减少进程中各任务并发执行时的时空开销，增加并发性

线程：轻量级进程，是进程中的一个实体，是程序执行流的最小单位。
- 线程是被系统**独立调度和分派**的基本单位
#### 线程和进程的比较
**进程与线程在资源占有上的差异**：
- 线程自己不拥有资源（除了在允许中必不可少的资源
- 线程共享进程的全部资源
#note 引入线程后，线程成为调度（CPU资源）的基本单位，但资源分配（除CPU）的基本单位仍然是进程
1. 调度：线程切换的代价低于进程
	- 同一进程，线程的切换不会引起进程切换
	- 不同进程的线程切换会运气进程切换
2. 独立性：
	- 进程除了共享全局变量，不允许其他进程访问地址空间
	- 同意进场的不同线程共享进程的地址空间和资源
3. 系统开销
	![[Pasted image 20250611145711.png]]


#### 线程的属性
 - 线程有唯一的标识符和一个[[#线程控制块]]
	线程控制块记录寄存器和栈等现场状态
 - 不同的线程可以执行相同的程序，即同一个服务程序被不同的用户调用时，操作系统将它们创建成不同的线程
- 线程之间（不同进程/同一进程）可以并发运行，在多CPU的计算机系统中，各线程可占用不同的CPU
#qes 为什么线程有助于提高系统并发性？
	线程切换时不一定引起进程切换，因此切换所需的开销变小，因此可以让更多的线程参与并发

#### 线程的状态与转换
与进程一样，略过

#### 线程的组织与控制
##### 线程控制块（TCB）
1. 线程标识符（TID）
2. 一组寄存器：程序计数器，状态寄存器，通用寄存器
3. 堆栈指针
4. 线程运行状态
5. 优先级
##### 线程的创建
![[Pasted image 20250611151654.png]]
##### 线程的终止
#qes 线程被终止后是否立即释放它所占有的资源？
	不释放
#qes 线程终止后什么时候释放资源？
	进程中的其他线程执行分离函数，此时被终止线程与资源分离
#qes 若线程已终止但资源未释放，它还能被其他线程做什么？  
	可被调用以**重新恢复运行**（再次利用其资源）
### 线程的实现方式
#### 用户级线程
**用户级线程**：应用程序模拟出多线程功能，线程管理由应用程序负责，无需操作系统干预
#note 操作系统意识不到线程的存在
	#note 调度依然以进程为单位（即使进程内有很多线程，依然分配同样的时间片

**优点**：
- 线程切换不需要到内核空间，效率高
- 不同进程可根据自身需要，对自己的线程选择不同的调度算法（应用程序实现线程
- 用户级进程的实现与操作系统无关，线程管理代码是用户程序的一部分
**缺点**：
- 当用户级线程阻塞时，进程内所有线程都被阻塞
- 线程运行于分配给进程的CPU，多个线程不能在多核CPU上并行运行（操作系统意识不到线程
#### 内核级线程
**内核级线程**：操作系统直接支持的线程，内核根据[[#线程控制块（TCB）|线程控制块]]控制线程

**优点**：
- 同一进程内的多个线程并行执行
- 一个线程被阻塞后，可以调用别的线程执行
**缺点**：
- 线程切换（同一进程/不同进程）需要从用户态到内核态执行，系统开销大
#### 组合方式
内核支持内核级线程，同时允许用户程序管理用户级进程
线程库 #todo 
#### 多线程模型
同时支持用户级线程和内核级线程的系统，根据它们之间的连接方式，可分为三种不同的多线程模型
##### 多对一模型
**多对一模型**：多个用户级线程映射到一个**内核级线程**，一个进程只能被分配一个内核级线程

**优点**：线程管理在用户态进程，无需切换到内核态，效率高
**缺点**：
- 若一个线程发生阻塞，则整个进程都被阻塞
- 多个线程不能同时在多个CPU上运行
##### 一对一模型
**一对一模型**：一个用户级线程映射到一个内核级线程

**优点**：一个线程被阻塞后，允许调度另一个线程运行，并发能力强
**缺点**：每一个用户线程都要创建一个内核线程，开销大
##### 多对多模型
**多对多模型**：n个用户级线程映射到m个内核级线程（n≥m）

**特点**：克服了**多对一模型**并发度不高的缺点，克服了**一对一模型**一个用户进程占用太多内核级线程，开销太大的缺点
# CPU调度
## 调度的概念
##### 调度的基本概念
>CPU调度：对CPU进行分配-从就绪队列中按照一定算法选择一个进程并将CPU分配给它执行

#### 调度的层次
##### 高级调度
**高级调度（作业调度）**：==创建态→就绪态==
	从外存中的后备队列选择合适的作业，把它们调入内存，分配资源并建立相应的进程 
	
#note 每个进程只调入一次，调出一次
	调入时会建立PCB，调出时会撤销PCB--是进程创建和消亡的过程
##### 低级调度
**低级调度（进程调度）**：==就绪态→运行态==
	按照某种算法从就绪队列中选择一个进程，分配CPU
##### 中级调度
**中级调度（内存调度）**：==挂起态↔就绪态==
	**选择部分进程换出到外存**此时为`挂起态`；
	当系统资源允许或需要时，再将它们“换入”回内存,并修改其状态为`就绪态`。

| 调度类型     | 调度对象          | 进程状态                | 主要作用         |
| -------- | ------------- | ------------------- | ------------ |
| **高级调度** | 作业（外存）→进程（内存） | 无→创建态→就绪态           | 决定“哪些作业能进内存” |
| **中级调度** | 进程（内存↔外存）     | 挂起态→就绪态<br>阻塞挂起→阻塞态 | 调整在内存中的进程数   |
| **低级调度** | 就绪进程（内存）      | 就绪态→运行态             | 决定“哪个进程运行”   |

## 调度的实现
### 调度程序
调度程序：调度和分派CPU
	调度算法-让谁运行
	时间片大小-运行多长事件
##### 调度程序组成
1. 排队器
2. 分派器：将CPU分派给新进程
3. 上下文切换器
### 调度的时机、切换、过程
#note 调度程序是操作系统**内核程序**
- 不能进行进程调度与切换的情况
	- 中断处理过程中
	- 屏蔽中断的原子操作过程中

#### 进程调度与切换的情况
- 可能进行进程调度与切换的情况
	- 进程获得竞争CPU资格
		- 创建新进程后
		- I/O设备准备就绪后，发出I/O中断，等待I/O的进程由阻塞态变为就绪态，此时应决定选择`新的就绪进程`/`中断发生时运行的进程`执行
	- 进程处理机结束占用
		- 进程正常结束/异常终止后
			若没有就绪进程则运行闲逛进程
		- 进程阻塞时
#### 不能进程调度与切换的情况
- 不能进行进程调度与切换的情况
	- 中断处理过程中
	- 屏蔽中断的原子操作过程中
### 进程调度相关概念
##### 进程调度的方式
###### 非抢占调度方式

###### 抢占调度方式
抢占调度方式：当一个进程在CPU上执行时，若有更重要/更紧迫的进程需要使用CPU，允许CPU暂停正在执行的进程，分配给该进程
##### 闲逛进程
闲逛进程：进程切换时，系统中没有就绪程序，就会运行闲逛进程。只要有进程就绪，就会立即让出CPU
#qes 闲逛进程让出CPU是抢占式还是非抢占式？
	抢占式

#note 闲逛进程不会被阻塞
#### 两种线程的调度
- 用户级线程调度：选择一个进程给予时间片，由进程自行决定让哪个线程运行
- 内核级线程调度：内核选择一个线程运行并给予时间片
## 调度的目标
- CPU利用率
- 系统吞吐量
- 周转时间：周转时间=作业完成时间-作业提交时间
	$带权周转时间=\frac{作业周转时间}{作业实际运行时间}$
- 等待时间
- 响应时间：用户提交请求到系统首次产生响应所用的时间
## 进程切换
##### 上下文切换
上下文切换：保存当前进程状态并恢复另一个进程的状态，实现进程切换
#note 上下文切换是实现进程调度的主要手段
#todo 
## CPU调度算法
#### 先来先服务调度算法
FCFS调度算法即可用于**作业调度**又可用于**进程调度**
FCFS调度算法属于**不可剥夺**算法
- FCFS调度算法的特点：
	- 算法简单，但效率低
	- 有利于长作业，不利于短作业
	- 有利于CPU繁忙型作业，不利于I/O繁忙型作业
#### 短作业优先调度算法
短作业优先算法若未特殊说明，默认为**非抢占式**的
	短作业优先算法可以是抢占式的，抢占式SPF算法又称为**最短剩余时间**优先调度算法

短作业调度算法的平均**等待时间**、平均**周转时间**是最优的

**短作业优先算法优缺点**
- 优点：最短的平均等待时间，平均周转时间
- 缺点：
	- 对短作业有利，对长作业不利
	- 可能导致饥饿现象
	- 作业的长短由用户提供，不一定真实
#### 高响应比优先调度算法
$响应比=\frac{等待时间+要求服务时间}{要求服务时间}$
#note 高响应比优先调度算法为非抢占式的算法，只有当前运行的程序主动放弃CPU时，才进行调度

特点：
- 要求服务时间越短，等待时间越长，相应比越高
- #note 克服了饥饿现象（等待时间越长相应比越高
#### 优先级调度算法
按照是否可以抢占可分为：
-  非抢占式优先级调度算法
- 抢占式优先级调度算法
按照优先级是否可改变：
- 静态优先级：优先级在创建进程时确定，在进程的整个运行期间保持不变
- 动态优先级：创建进程时赋予一个优先级，优先级随着需求而改变以获得更好的调度性能

优先级设置原则：
- 系统进程>用户进程
- 交互式进程>非交互式进程
- IO型进程>计算型进程
	使I/O设备尽早开始工作，提高系统的整体效率


#### 时间片轮转调度算法

#### 多级队列调度算法
#### 多级反馈队列调度算法
# 同步和互斥
## 同步和互斥的基本概念
#### 同步
>**同步**：为协调多个进程在特定点的**执行顺序**而产生的**制约关系**，以保证它们对共享资源的操作能够按照某种预定的顺序进行
	又称为直接制约关系

#eg ![[Pasted image 20250609134719.png]]

#### 互斥
互斥：对临界区必须互斥访问
	又称为间接制约关系

#### 临界资源
>临界资源：一次仅允许一个进程使用的资源

>临界区：访问临界资源的代码
###### 临界资源的访问过程
1. 进入区：检查是否可进入临界区，若能进入则设置标志
2. 临界区：访问临界资源的代码
3. 退出区：将访问临界资源的标志清除
4. 剩余区

``` c
while (true) {  
    entry section;          // 进入区  
    critical section;       // 临界区  
    exit section;           // 退出区  
    remainder section;      // 剩余区  
}
```
###### 同步机制准则
1. 空闲让进
2. 忙则等待
3. 有限等待：
4. 让权等待：不能进入临界区时，立即释放处理器，防止进程忙等

## 实现临界区互斥的基本方法
### 软件实现方法
#### 单标志法
单标志法：设置公用变量turn，用于指示`可以进入临界区`的**进程编号**。进程退出临界区时，将变为另一个进程的编号。
	算法思想：turn变量只能对应一个进程访问临界区，因此可以实现互斥访问
	访问完临界区的进程后会把权限转交给另一个进程，每个进程进入临界区的权限只能被另一个进程赋予
	#note 若某个进程不在进入临界区，则另一个进程也无法进入临界区，违背==空闲让进原则==

- 进程$P_{0}$
	``` c  
	while (turn!=0);   //进入区，循环检查--此时一直占用处理机，违反让权等待
	critical section;
	turn = 1;    //退出区，把权限给另一个进程
	remainder section;
	```
- 进程$P_{1}$
``` c  
	while (turn!=0);   //进入区，循环检查--此时一直占用处理机，违反让权等待
	critical section;
	turn = 0;    //退出区，把权限给另一个进程
	remainder section;
	```
#### 双标志法
- 先查后锁→都使用
- 先锁后查→都被锁
##### 双标志先检查法
**双标志先检查法**：设置布尔型数组flag[2]，标记进程想要进入临界区的意愿。
	进入区：先检查对方是否想进入临界区，若想则等待。否则将flag[i] 置为true,进入临界区。
	退出区：将flag[i]=false，表达自己不再使用
#note 非原语实现，会造成两个进程同时进入临界区，违反==忙则等待原则==
	#qes 什么情况下两个进程同时进入临界区？
		一方检查后还没设置flag，就被调度到另外一方，则双方都通过检查

- 进程$P_{0}$
``` c  
	while (flag[1]);   //进入区，循环检查对方--此时一直占用处理机，违反让权等待
	flag[0]=true;      //进入区，表达意愿
	critical section;
	flag[0]=false;     //退出区，把权限给另一个进程
	remainder section;
	```

- 进程$P_{1}$
``` c  
	while (flag[0]);   //进入区，循环检查对方--此时一直占用处理机，违反让权等待
	flag[1]=true;      //进入区，表达意愿
	critical section;
	flag[1]=false;     //退出区，把权限给另一个进程
	remainder section;
	```
##### 双标志后检查法

**双标志先检查法**：设置布尔型数组flag[2]，标记进程想要进入临界区的意愿。
	进入区：先设置自己的标志flag[i]=true，再检查对方标志，若为true则等待，否则进入临界区。
	退出区：将flag[i]=false，表达自己不再使用
#note 非原语实现，会造成两个进程同时被锁住，违反==空闲让进原则==，违背==有限等待原则==
	#qes 什么情况下两个进程同时被锁住？
		一方设置flag后，就被调度到另外一方，则双方都flag都为1，无法通过while循环检查

- 进程$P_{0}$
``` c  
	flag[0]=true;      //进入区，表达意愿
	while (flag[1]);   //进入区，循环检查对方--此时一直占用处理机，违反让权等待
	critical section;
	flag[0]=false;     //退出区，把权限给另一个进程
	remainder section;
	```

- 进程$P_{1}$
``` c  
	flag[0]=true;      //进入区，表达意愿
	while (flag[1]);   //进入区，循环检查对方--此时一直占用处理机，违反让权等待
	critical section;
	flag[1]=false;     //退出区，把权限给另一个进程
	remainder section;
```

##### Peterson算法
Peterson算法：先用flag[]表达意愿，turn标志用于真正确定由谁访问

### 硬件实现方法
##### 中断屏蔽方法
>硬件屏蔽方法：先关中断，在临界区访问完再开中断

缺点：
1. 不适用于多处理机系统，因为关中断不能防止其他CPU执行临界区代码
2. 不适用于用户进程，不能随意让用户使用
#### 硬件指令方法
硬件指令采用原语，来保证指令的一气呵成
##### TestAndSet指令
TestAndSet指令：读出标志同时将该标志设置为真
	根据读出的old标志判断能否访问
	设置标志表达有进程正在访问临界区（自己/其它进程）

``` c  
boolean TestAndSet(boolean *lock) {  
    boolean old;  
    old = *lock;  
    *lock = true;  
    return old;  
}
```

TS指令实现互斥的过程
``` c  
while TestAndSet(&lock);  //循环检查lock的值，无法让权等待
critical section;
lock =  false;  // 释放锁
remainder section;
```
##### Swap指令
Swap指令的功能是交换两个字的内容
``` c  
Swap(boolean *a,boolean *b) {  
    boolean temp = *a;  
    *a = *b;  
    *b = tmp;  
}
```
实现思想：定义变量key取得lock的值，同时设置lock为真，循环检查key是否为false--满足进入临界区的条件
	根据key判断能否访问
	设置lock表达有进程正在访问临界区（自己/其它进程）
Swap指令实现互斥的过程
``` c  
boolean key = true;
while(key!=false){  //循环检查lock的值，无法让权等待
	Swap(key,*lock);
}
critical section;
lock =  false;  // 释放锁
remainder section;
```

#### 硬件指令方法的优缺点
- 优点：
	1. 实现简单
	2. 支持多处理机环境，适用于任意数量的进程
	3. 支持多个临界区，只需每个临界区设置一个布尔常量
- 缺点：
	1. 不能实现让权等待（while不断检查
	2. 随机选择进程进入临界区，可能导致饥饿
### 互斥锁
**互斥锁**：变量available表示锁是否可用，一个进程在进入临界区时调用acquire()函数获得锁，退出临界区时调用releaase()函数释放锁
#note acquire()和releaase()是原子操作

``` c  
acquire(){
	while(!available);  // 忙等待
	available = false;  // 获得锁
}
release(){
	available=true;
}
```
缺点：不能实现让权等待
#note 不适合单处理机系统
	#qes 为什么不适合单处理机？
		一次只能运行一个进程，在忙等待时，占有临界区的资源无法被调度释放临界区资源，不可能解锁

#qes 如何确保空闲让进和忙则等待？
	#note 使检查和上锁一气呵成，因此大多使用原语解决/Peterson 算法

### 信号量
>**信号量机制**：定义一个变量表示系统内**可用资源**的数量，通过wait()原语和signal()原语对信号量操作，便于实现进程互斥和同步

#### 整型信号量
整型信号量：用一个`整型量S`表示某种资源的数量
#note 仍使用循环检查，没有实现**让权等待原则**
``` c  
// wait操作：等待S资源，若有则占用一个(S=S-1)
wait(S){
	while(S<0);
	S=S-1;
}
// signal操作，归还一个资源
signal(S){
	S=S+1;
}
```

#### 记录型信号量
>**记录型信号量**：使用`整型量S`表示某种资源的数量，再增加一个`等待进程列表L`，用于链接等待该资源的进程

>记录型信号量数据结构
#note 把所有等待的进程放进队列里，当有资源时再进行唤醒。不再轮询检查导致忙等
``` c 
typedef struct {  
    int value;  
    struct process *L;  
} semaphore;
```

>记录型信号量相关操作
- 当value为负时，表示有多少进程在等待队列
- 当value为负时，表示有多少系统资源
``` c 
void wait(semaphore S){
	S.value--; // 
	if(S.value<0){
		// 进程阻塞
		add this process to S.L;
		block (S.L);
	}
}
```

``` c 
void signal(semaphore S){
	S.value++;
	if(S.value<=0){
		// 从链表L唤醒一个进程
		remove a process P from S.L;
		wakeup(P);
	}

}
``` 
#### 信号量的应用
##### 利用信号量实现互斥
``` C
semaphore S = 1;
P1(){
	P(S); // P实则是S--和若没有资源则加入阻塞队列
	critical section;
	V(S);
}
``` 

##### 利用信号量实现同步
``` C
semaphore S = 0;
P1(){
	...
	V(S);
}

P2(){
	...
	P(S);
}
``` 
思想：通过生产资源(V)和消费资源(P)的前后顺序来实现`同步关系`，即必须`先V后P`
##### 利用信号量实现前驱
1. 为每一对前驱关系设置一个同步信号量
2. 节点V出度的信号量（必须每个入度信号量都到达
3. 节点P入度的信号量（同时提供多个出度信号量
![[102340817494606862.png|500]]

### 经典同步问题
#todo 
#### 生产者消费者问题
#### 读者-写者问题
#### 哲学家进餐问题

### 管程
管程：通过共享数据结构表示系统内的共享资源，提供一组函数对该数据结构进行操作。自动实现互斥，配合条件变量实现同步。
###### 管程的组成

| 组成部分       | 说明                                                            | ddddddddddddddddddd |
| ---------- | ------------------------------------------------------------- | ------------------- |
| **共享数据结构** | 要保护的数据资源（如缓冲区、计数器等）                                           |                     |
| **操作过程**   | 一组对共享变量的操作函数（如 `insert()`, `remove()`）                        |                     |
| **同步机制**   | 条件变量（Condition Variable）+ 原语（如 `wait()` 和 `signal()`）用于同步进程行为 |                     |


###### 管程的基本特征
1. 管程将对共享资源的操作封装起来，必须通过管程提供的过程才能访问共享资源
2. 每次仅允许一个进程调用管程提供的函数，因此该过程自动实现互斥
#qes 为什么管程可以自动实现互斥访问？


### 🧩 二、管程的组成

一个管程通常包括以下三部分：

| 组成部分     | 说明                                                            |
| -------- | ------------------------------------------------------------- |
| **共享变量** | 要保护的数据资源（如缓冲区、计数器等）                                           |
| **操作过程** | 一组对共享变量的操作函数（如 `insert()`, `remove()`）                        |
| **同步机制** | 条件变量（Condition Variable）+ 原语（如 `wait()` 和 `signal()`）用于同步进程行为 |
#### ✅ 互斥：

- 管程**自动保证互斥**：一次只能有一个线程进入执行其内部的过程（不需要程序员显式加锁）。
#### 🔁 同步：
- 管程使用**条件变量**来实现同步：
    - `wait(cond)`：当前进程等待某个条件变量 `cond`，并释放管程控制权；
    - `signal(cond)`：唤醒一个在`cond` 上等待的进程；
    - `broadcast(cond)`：唤醒所有等待 `cond` 的进程（某些实现中）。

| 比较项  | 管程                 | 信号量                |
| ---- | ------------------ | ------------------ |
| 互斥控制 | **自动实现**互斥（只需调用过程） | 需手动编写 `P` / `V` 操作 |
| 同步表达 | 更**结构化**，逻辑清晰      | 容易出错（顺序错会导致死锁）     |
| 复杂程度 | 更高级、语义更强           | 更底层、更灵活            |
| 应用难度 | 适合写结构化并发程序         | 适合底层控制或系统实现        |

# 死锁
## 死锁的概念
**死锁**：多个进程因争夺资源而造成的一种僵局（**互相等待**对方的资源），若无外力干涉，这些进程都无法推进

#### 死锁产生的原因
1. 系统资源的竞争：**不可剥夺资源**的数量不足以满足多个进程的需要，使得进程因争夺资源陷入僵局
	#note 对可剥夺资源的竞争不会引起死锁
2. 进程推进顺序非法
#### 死锁产生的必要条件


