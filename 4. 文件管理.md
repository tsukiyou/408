# 文件系统基础
## 文件的基本概念
- 用户的输入输出中，以文件为基本单位
	应用程序的输入使通过文件实现的，输出也保存在文件里

##### 文件的属性
- 名称
- 类型
- 创建者
- 所有者
- 位置
- 大小
- 保护
- 创建信息，最后一次修改时间和最后一次存取时间

## 文件的操作
##### 文件的基本操作
操作系统提供了一系列的系统调用，实现对文件的操作
- 创建文件
	![[Pasted image 20250615153801.png|550]]
- 删除文件
	![[Pasted image 20250615153850.png|550]]
- 读文件
	![[Pasted image 20250615153914.png|550]]
- 写文件
	![[Pasted image 20250615153939.png|550]]
#### 文件的打开与关闭

###### 打开文件表：包含所有打开文件信息的表
- 系统的打开文件表：与进程无关的信息
	- 文件在磁盘的位置
	- 打开计数
- 进程的打开文件表：进程对文件的使用信息
	- 文件指针：上次读写位置的指针
	- 访问权限
	- 系统表索引号：进程的文件条目指向系统的打开文件表中的条目
		当打开文件时，进程的打开文件表条目指向系统打开文件表
![[64550917500588112(1) 1.png|500]]

#### 文件打开与关闭
**文件打开**：`系统调用open`
	当打开一个文件时将该文件目录项从外存复制到内存的`打开文件表`的一个表目中，并将该表目的`索引号`返回给用户
	#note 文件打开只是将==目录项==复制到打开文件表，并不是把该文件直接调入内存
**文件关闭**：`系统调用close`
	删除单个`进程的打开文件表`的相应条目
	若`打开计数器`为0，则从`系统打开文件表`中删除这一表目

#note 文件打开后对文件的操作基于什么标志进行
	文件进入打开文件表后，通过文件描述符进行**索引**，不通过文件名访问，所有文件操作通过==文件描述符==（文件句柄）进行
	#note 文件名可不是打开文件表的一部分（通过文件描述符**索引**


## 文件的结构
### 文件的逻辑结构
##### 无结构文件
==无结构文件==：由字符流构成的文件，也称为流式文件，长度以字节为单位
#qes 长度以什么为单位？
#qes 无结构文件怎么搜索文件记录？
	没有结构，只能通过穷举搜索的方式
#### 有结构文件
==有结构文件==：由一组相似的记录组成，每条记录由若干个数据项组成，一般每条记录设立一个关键字

根据各记录的**长度**是否相等，可分为
- 定长记录 #note 可以随机存取
- 变长记录
	#note 只能顺序查找，速度慢

###### 顺序文件
文件中的记录一个一个的顺序排列，记录可以使定长记录也可以是变长记录

- 顺序文件记录排列的两种结构：
	- **串结构**：记录间的顺序与关键字无关，通常按存入的先后顺序进行排列
	- **顺序结构**：所有记录按关键字排序
		顺序结构**按照关键词**查找时可采用折半查找，效率高
		#note 增删时需要移动大量记录，较为困难

###### 索引文件
#qes 索引表的思想？
	采用索引表将对变长记录顺序文件的顺序检索变为对定长记录索引文件的随机检索
	变长记录的顺序文件因长度不定无法随机存取
**索引表**：一个**定长记录的顺序文件**，为主文件的每个记录在索引表中分别设置索引表项（包含记录的指针和记录长度），通过对索引查找快速访问文件
	![[Pasted image 20250615160126.png|400]]

###### 索引顺序文件
为**顺序文件**建立一张索引表，先把所有记录分成若干组，索引表中加入由每组第一个记录建立的一个索引项
	![[Pasted image 20250615165604.png|450]]
- 先组间查找，确定组后再组内查找，可以有效提高查找速度
###### 直接文件或散列文件
#todo 
### 文件的物理结构
#### 连续分配
连续分配：文件在磁盘上占有一组连续的块，**目录项**中记录该文件`第一个磁盘块`的块号和所占用的块数
	物理块号=起始块号+逻辑块号
	物理地址=物理块号地址+块内地址
	![[Pasted image 20250616143138.png|250]]

- 连续分配的优点：
	- 支持顺序访问和**直接访问**
	- 访问速度快（文件占用的块相邻，磁头移动距离小--物理上
- 连续分配的缺点：
	- 要为一个文件分配连续的存储空间，会产生很多[[3. 内存管理#^89c048|外部碎片]]
	- 必须事先知道文件的长度（安排相应大小的物理块
	- 无法满足文件动态增长的要求
		- 插入和删除需要移动大量记录

#### 链接分配
##### 隐式链接
**目录项**中含有文件`第一块的指针`和`最后一块的指针`，每个盘块都存有指向文件**下一个盘块的指针**，磁盘块可任意分布在磁盘，通过指针找到下一块
	![[102086817499754582(1).png|250]]
#note 指针对用户是透明的

#sp 隐式链接按簇链接
	将几个盘块组成一个簇，按簇分配（即原本盘块由盘块簇代替
	**缺点**：增加了内部碎片
	#qes 为什么按簇链接会增加内部碎片？
		储存时的单位是块，而若没有填满最后一个簇则形成了内部碎片

- 隐式链接**缺点**：
	- 只支持顺序访问
	- 任何一个**指针**出问题都会导致文件数据丢失，稳定性低
	- 指向下一个盘块的**指针**要耗费一定的存储空间
##### 显式链接
将链接文件各物理块的**指针**，显式的存放在内存的一张链接表中，称为`文件分配表`
**文件目录**中只需要记录起始块号，`文件分配表`表项由`盘块号`和`下一块指针`构成。
	![[Pasted image 20250616222702.png|350]]
	#note 显示链接方式，一个磁盘只有一张**FAT**，FAT的表项与`全部磁盘块`一一对应
		通过`目录`中的`起始块号`找到文件初始位置，-1表示该文件的最后一块，-2可表示该磁盘块空闲
#### 索引分配
**索引分配的思想**：把文件的盘块号集中的放在一起，当访问到某个文件时，将该文件对应的盘块号（即索引表）一起调入内存 

**索引分配**：为每个文件分配一个`索引块（索引表）`，将分配给该文件的所有盘块号都记录在该索引块中
##### 单级索引分配方式
目录记录`索引块号`，==索引块结构==由`逻辑块号`和`物理块号`构成，`物理块号`直接表明物理地址，可单级直接访问物理块
	![[Pasted image 20250616224756.png|300]]
	#note `逻辑块号`可隐含（表明该文件连续的单元(必然从0连续到最后-无意义)
#note 文件大小受**单个**索引块可容纳盘块号限制
	若要用多个索引块表示一个文件，则通过链指针将各索引块链接起来

索引分配**优缺点**：
- 优点：
	- 支持直接访问
	- 不会产生外部碎片
- 缺点：
	- 索引块增加了额外的空间开销
	- 文件很小时仍为其分配一个索引块（大量空余，索引块利用率低
##### 多级索引分配方式
文件太大而索引块太多时，可为索引块再建立一级索引，称为**主索引**，主索引中包含下一级索引块的盘块号。依次递归，直到最后一级索引直接记录磁盘号。
	![[Pasted image 20250617132536.png|450]]
多级索引优缺点：
- 优点：加快了对大型文件的查找速度
- 缺点：启动磁盘数随着**索引级数**的增加而增加
##### 混合索引分配方式
顶级索引表中，根据文件的大小不同，采用直接地址、一次间接地址、多次间接地址等方式
- **直接地址**：设置10个直接地址项，用i.addr(0)~i.addr(9)存放**直接地址**，如同一级索引寻址，可直接访问物理块
- **一次间接地址**：用i.addr(10)提供**一次间接地址**，指向索引块（终端索引），由索引块访问文件盘块
- **多次间接地址**：用i.addr(11)/i.addr(..n)提供**多次间接地址**，即在多级索引分配的基础上用顶级索引表指向不同级别的索引
![[Pasted image 20250617135712.png|450]]

### 文件存储空间管理
一个物理磁盘可分为多个分区，每个分区都有单独的文件系统，每个包含文件系统的分区称为==卷==
文件区：存放文件数据的空间
目录区：存放FCB的空间
![[Pasted image 20250617231031.png|400]]
#### 空闲表法
**空闲表法**：为所有空闲区建立一张空闲表，每个空闲区对应一个空闲表项，包括表项序号、`第一个空闲盘块号、空闲盘块数`等信息
	![[Pasted image 20250618095107.png|300]]

盘块的分配与回收类似于内存回收的方法

优点：具有较高的分配速度，可减少磁盘I/O的频率，较小的文件可连续分配相邻的盘块
#### 空闲链表法
空闲盘块链：每个盘块中有指向下一个盘块的指针，将所有空闲空间以盘块为单位拉成一条链
	![[67938017500567492(1).png|250]]
	优点：分配和回收一个盘块很简单
	缺点：
		为一个文件分配盘块时可能要操作多次
		以盘块为单位，盘块链会很长
空闲盘区链：多个相邻空闲`盘块`合并成为一个**盘区**，盘区中含有指向下一个空闲盘区的指针和`本盘区的盘块数`，将空闲空间以**盘区**为单位拉成一条链
	![[80988717500568452(1).png|250]]
	优点：分配和回收的效率较高
	缺点：分配和回收的过程复杂
#### 位示图法
位示图：通过行和列计算盘块号，用二进制0表示空闲，1表示已分配
	![[Pasted image 20250618144435.png|400]]
- 由盘块号计算行列
- 由行列计算盘块号

- 优点：
	- 容易在位示图中找到一个/一组相邻的空闲盘块
	- 位示图占用空间少，可保存在内存，减少I/O开销
- 缺点：位示图大小会随着磁盘空间的增大而增大
#### 成组链接法
每组由一组`指向空闲盘块`的**空闲盘块号**组成，每组的第一个盘块记录下一组`空闲盘块号`和空闲盘块`总数`，各组的第一个盘块可链接成一条链
	#note 第一组保存在内存的专用栈中，称为**空闲盘块号栈**
	#note 最后一组盘块指示位占据一个盘块，因此比别的分组少一个盘块
	![[Pasted image 20250618145047.png|600]]

###### 超级块
超级块：存放空闲盘块号栈，以及卷中的目录区、文件划分信息。
#note 在对卷进行操作前，超级块要预先读入内存，并经常保持内存与磁盘超级块的一致性

###### 盘块的分配
- 若为普通盘块：**指针**指向空闲盘块栈顶，分配盘块时执行`出栈`操作，`空闲盘块数`-1
- 若为==栈底盘块==（每组第一个盘块）：**调入**该盘块记录的下一组盘块进入`空闲盘块号栈`，**分配**该盘块，**指针**指向新的空闲盘块号栈的栈顶
###### 盘块的回收
- 若未满，盘块`入栈`，`空闲盘块数`+1
- 若已满，则将新回收的盘块号作为新栈底（指向旧盘块链，空闲盘块数置1）
# 目录
#### 文件控制块
文件控制块（FCB）：用来存放控制文件所需要的各种信息的数据结构
	#note FCB的有序集合称为**文件目录**，一个FCB就是一个**文件目录项**

- FCB的主要信息构成
	- 基本信息
	- 存取控制信息
	- 使用信息

#### 索引节点 
==索引节点==：将文件名和文件描述信息分离，使文件描述信息单独形成一个称为`索引节点`的数据结构。**目录项**仅由`文件名和索引节点号`组成 
	#qes 索引节点存放的是文件名还是文件描述信息？

#qes 文件目录的存放位置在哪里？
	文件目录通常存放在磁盘上
索引节点的引入思想：
	当查找一个目录项时，只需要逐步比较目录名，而不需要其他信息。冗余信息使得信息密度降低，需要多次调页
	引入索引节点后相同大小下密度更高，减少**调页**次数

磁盘索引节点：
	![[Pasted image 20250616141311.png|600]]
内存索引节点：
	![[Pasted image 20250616141345.png|600]]

## 目录的基本概念
FCB的有序集合称为**文件目录**，一个FCB就是一个**文件目录项**
目录管理要实现**按名存取**（用户角度看，目录再文件名与文件之间提供一种映射 ^1df7f7

## 目录的操作
- 搜索
- 创建文件
- 删除文件
- 创建目录
- 删除目录
- 移动目录
- 显示目录
- 修改目录

## 目录结构
在某个目录中都不允许重名
#### 单级目录结构
**单级目录结构**：整个文件系统中只建立一张目录表，每个文件占一个目录项
	![[Pasted image 20250615230056.png|400]]
- 特点：不适用于多用户的操作系统
	- 查找速度慢
	- 不允许重名
	- 不便于文件共享
#### 两级目录结构
**两级目录结构**：文件目录分为主文件目录和用户文件目录两级
	`主文件目录`记录`用户名`和`相应用户文件目录`所在的存储位置
	`用户文件目录`项记录`该用户所有文件的FCB`，之后没有其他目录
	![[Pasted image 20250615230412.png|400]]

优点：
- 提高检索速度
- 解决了多用户之间的重名问题
- 可以实现访问限制
缺点：
- 缺乏灵活性，不能对文件分类
#### 树形目录结构
**树形目录结构**：采用文件的路径名唯一标识一个文件，可无限制向下创建目录，每创建一个目录则在路径中用"/"拼接该文件名，用于指明目录
	![[Pasted image 20250615231604.png|400]]

###### 树形目录结构基于相对路径的访问
为每个进程设置一个`当前目录`，进程对各文件的访问都只需要对于当前目录进行。当用户要访问某个文件时使用`相对路径`标识文件

###### 树形目录结构的优缺点
- 优点
	- 方便对文件分类，层次结构清晰
	- 有效的对文件管理和保护
	- 易于赋予不同的存取权限
- 缺点
	- 查找文件需要逐级访问，增加了磁盘访问次数
#### 无环图目录结构
**无环图目录结构**：在树形目录结构的基础上增加一些指向同一节点（共享文件）的有向边，使整个目录成为一个有向无环图
	![[Pasted image 20250616113108.png|400]]
	

- 无环图目录结构通过`共享计数器`记录文件的共享用户数
	仅当共享计数器为0时，才删除该节点
#qes 无环图目录结构什么时候真正删除一个节点（共享计数器）？

## 文件保护
#### 访问类型
- 读/写
- 执行：将文件装入内存并执行
- 添加：添加新信息到文件结尾部分
- 删除
- 列表清单：列出文件名和文件属性
#### 访问控制
##### 访问控制列表
**访问控制列表（ACL）**：规定**每个用户名**及其允许的访问类型
**精简的**访问控制列表可采用**拥有者**、**组**、**其他**三种用户类型
	![[Pasted image 20250619114915.png|400]]
#qes 访问控制列表与精简访问控制列表的区别？
	访问控制列表对每个用户丢需要去顶访问类型
	精简访问控制列表把用户归到组内

##### 口令
用户在建立文件时提供一个口令，在建立PCB时存放口令在PCB，在访问文件时需要提供口令

优点：时间开销小
缺点：口令存放于系统内部，不安全
##### 密码
对文件进行加密，访问文件是需要使用密钥解密
	#note 存放的是加密后的数据，解密后才为原始数据

优点：保密性强，不需要存储密码
去欸但：加密/解密需要一定时间
## 文件共享
#### 基于索引节点的文件共享（硬链接）
**硬链接**：在目录中只设置`文件名`和指向相应**索引节点**的`指针`，文件的物理地址和属性等信息放在**索引节点**中，索引节点中使用`链接计数count`表示链接到本索引节点的用户目录项的数量
	![[Pasted image 20250618224705.png|400]]
#### 利用符号链实现文件共享（软链接）
**软链接**：在目录中创建一个 **类型为 LINK** 的新文件 L，L 的 `inode` 数据区存放目标文件 **F 的路径字符串**，内核解析该字符串，根据L中的路径逐级查找目录项→最终定位到 F 的**目录项**→获取 F 的 **inode**→访问文件内容
	![[Pasted image 20250618232347.png|450]]
	#note 链接也是一个文件，需要先找到其索引节点才能读出路径字符串，需要耗费一定的时间
	#note 依据路径名依次查找目录，会导致多次读盘，增大开销
	#note 软链接可实现网络文件共享：提供网络地址和文件路径名

# 文件系统
## 文件系统结构
#todo 
## 文件系统布局
## 虚拟文件系统
**虚拟文件系统（VFS）**：屏蔽了不同文件系统的差异，**向上**提供统一的调用接口，**向下**要求下层文件系统实现其规定的函数
	![[33362217500640632(1).png|400]]
#qes [[虚拟文件系统vnode和inode抽象实现分析#1 为什么 VFS 需要“通用文件对象”？|为什么 VFS 需要“通用文件对象”]]
#### 虚拟文件系统实现方式
[[虚拟文件系统vnode和inode抽象实现分析]]

- [[4. 文件管理#超级块|超级块]]对象 **`struct super_block`** ：对应磁盘上特定扇区的文件系统超级块
- 索引节点对象 **`struct file`**
	
- [[4. 文件管理#^1df7f7|目录项]]对象 **`struct dentry`** 
- 文件对象  **`struct file`**
	  每打开一个文件，VFS就在主存中新建一个`struct file`指向`struct inode`，`struct inode`用统一的数据结构表示文件，==**`struct inode`**== 包含`文件信息`和`函数指针`
		若对应的 **`struct inode`**（索引节点对象）**已在内存缓存**中，则仅把引用计数 +1；  
		    若缓存中不存在，才从底层文件系统读入并缓存一个新的 inode。
		[[inode和struct inode的区别]]
		`struct inode`只存在于**主存**中，磁盘 **`inode`** 既会被调入主存，也会在**外存**
##### 典型访问流程
namei → dentry cache          (路径解析)
        ↓
  (若不存在) 读取磁盘 inode → 创建 struct inode → 填充 i_op / i_fop
        ↓
  创建 struct file            (每次 open 都要)

> **记忆口诀**
> - inode：永远在磁盘，静态数据。
> - `struct inode`：只在内存，附带方法。
> - 打开文件永远新建 `struct file`，但 **不一定**新建 `struct inode`。

###### 虚拟文件系统调用过程实例
当进程发起一个面向文件的系统调用时，内核调用VFS的一个函数，该函数调用目标文件系统的相应函数

#note VFS只存在于内存中，
	VFS在系统启动时建立，在系统关闭时消亡

## 文件系统挂载
**文件系统挂载**：文件系统在进程使用之前必须先安装，也称挂载，将文件系统挂载到某个目录，就可通过该目录访问设备上的文件

安装点：安装文件系统的**目录**
	#note 同一个设备可以有多个安装点，同一个安装点同时只能**挂载**一个设备
